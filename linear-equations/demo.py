from linear_equations import LinearEquation

print('line = LinearEquation.from_points((1, 2), (3, 3))')
line = LinearEquation.from_points((1, 2), (3, 3))
print('     =', line)
print(f'{line(0) = }')
print(f'{line(5) = }')
